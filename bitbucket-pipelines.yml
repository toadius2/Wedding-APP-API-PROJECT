pipelines:
  default:
    - step:
        script: # Modify the commands below to build your repository.
          - apt-get update
          - apt-get -y install zip openssl curl -y
          - bash ./upload.sh